package com.payflow.enums;

/**
 * Supported settlement currencies. PayFlow operates in Indian Rupee; the enum keeps the
 * door open for multi-currency support without a schema change.
 */
public enum Currency {
    INR
}
